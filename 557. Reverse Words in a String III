class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        strings = s.split()
        for i in range(len(strings)):
            strings[i] = strings[i][::-1]
        return ' '.join(strings)
